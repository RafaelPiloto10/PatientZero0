class Country{
  constructor(num_states, init_funds, init_ppe) {
    this.num_states = num_states;
    this.funds = init_funds;
    this.ppe = init_ppe;
    
    for(let i = 0; i < this.num_states; i++){
      
    }
  }
}